-- «Удалить после прочтения» так и не появилось: колонку никто не
-- выставлял, а обработчик прочтения нигде не вызывался. Срок исчезающего
-- сообщения считается от отправки.
ALTER TABLE message_expiry DROP COLUMN IF EXISTS auto_delete_on_read;
